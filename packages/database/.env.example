# Local: misma URL que apps/api/.env.development (docker compose en la raíz, puerto 5433).
# Fly: no uses este archivo; exporta DATABASE_URL o usa fly ssh console + prisma migrate.
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5433/trofoschool?schema=public"
