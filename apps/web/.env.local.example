# Copia a apps/web/.env.local (gitignored). Tiene prioridad sobre .env.development en `next dev`.
# `NEXT_PUBLIC_*` de aquí pisan los de apps/web/.env.development si coinciden las claves.

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_STRIPE_PUBLIC_KEY="pk_test_..."

# Opcional en servidor (middleware / server components)
CLERK_SECRET_KEY="sk_test_..."
